.DS_Store
__pycache__/
*.pyc

# build artifacts (keep-local, never committed)
build/
dist/
*.egg-info/
.pytest_cache/

# working files: session audit trails and notes bound to one enforcement
# host's internal implementation. Kept on disk, never part of the package.
work/
